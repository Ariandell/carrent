tailwind.config = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                border: "var(--divider)",
                input: "var(--input-bg)",
                ring: "var(--primary-glow)",
                background: "var(--bg-core)",
                foreground: "var(--text-primary)",
            }
        }
    }
}
